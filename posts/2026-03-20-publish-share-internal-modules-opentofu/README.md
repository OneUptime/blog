# How to Publish and Share Internal Modules for OpenTofu

Author: [nawazdhandala](https://www.github.com/nawazdhandala)

Tags: OpenTofu, Terraform, Module, Module Registry, GitOps, Internal Sharing

Description: Learn how to publish and share internal OpenTofu modules using Git repositories, private module registries, and semantic versioning for team-wide consumption.

## Introduction

Sharing modules across teams requires a publishing mechanism and a clear versioning strategy. OpenTofu supports referencing modules from Git repositories, private registries, and S3/GCS buckets. Git tags with semantic versioning is the most widely adopted internal sharing approach.

## Git-Based Module Sharing

### Tagging Module Releases

```bash
# In your module repository

git tag -a v1.0.0 -m "Initial stable release of VPC module"
git tag -a v1.1.0 -m "Add support for IPv6 subnets"
git tag -a v2.0.0 -m "Breaking: rename subnet variables for consistency"
git push origin --tags
```

### Consuming Git-Tagged Modules

```hcl
# Reference a specific tag for production stability
module "vpc" {
  source = "git::https://github.com/your-org/tofu-modules.git//modules/vpc?ref=v2.1.0"

  name        = "prod-vpc"
  cidr_block  = "10.0.0.0/16"
  environment = "prod"
}

# Reference a branch for development (not recommended for prod)
module "vpc_dev" {
  source = "git::https://github.com/your-org/tofu-modules.git//modules/vpc?ref=feature/ipv6-support"
  # ...
}
```

## Module Repository Structure

Organize your module monorepo so each module has a stable path within the repository:

```text
tofu-modules/
├── modules/
│   ├── vpc/
│   │   ├── main.tf
│   │   ├── variables.tf
│   │   ├── outputs.tf
│   │   ├── versions.tf
│   │   └── README.md
│   ├── database/
│   ├── ecs-service/
│   └── security-group/
├── examples/
│   ├── complete-vpc/
│   └── simple-ecs/
├── tests/
└── CHANGELOG.md
```

## Module Documentation with terraform-docs

```markdown
# modules/vpc/README.md (auto-generated by terraform-docs)
# Run: terraform-docs markdown table --output-file README.md --output-mode replace .

## Requirements

| Name | Version |
|------|---------|
| terraform | >= 1.6.0 |

## Inputs

| Name | Description | Type | Default |
|------|-------------|------|---------|
| name | VPC name | string | n/a |
| cidr_block | VPC CIDR range | string | n/a |
```

## Automated Release with GitHub Actions

```yaml
# .github/workflows/release-module.yml
name: Release Module

on:
  push:
    tags:
      - 'v*'

jobs:
  validate:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        module: [vpc, database, ecs-service, security-group]
    steps:
      - uses: actions/checkout@v4
      - uses: opentofu/setup-opentofu@v2

      - name: Validate module
        run: |
          cd modules/${{ matrix.module }}
          tofu init -backend=false
          tofu validate

      - name: Check docs
        uses: terraform-docs/gh-actions@v1.4.1
        with:
          working-dir: modules/${{ matrix.module }}
          output-file: README.md
          output-method: replace
          fail-on-diff: "true"

  release:
    needs: validate
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - name: Create Release
        run: gh release create "${{ github.ref_name }}" --title "Release ${{ github.ref_name }}" --notes-file CHANGELOG.md --verify-tag
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
```

## Private Registry Setup

For larger organizations, use a private Terraform/OpenTofu registry:

```hcl
# modules/vpc/versions.tf
terraform {
  # Declare what this module requires
  required_version = ">= 1.6.0"
  required_providers {
    aws = {
      source  = "hashicorp/aws"
      version = ">= 5.0"
    }
  }
}
```

```hcl
# Consuming from a private registry
module "vpc" {
  source  = "registry.your-org.com/networking/vpc/aws"
  version = "~> 2.0"
  # ...
}
```

## Conclusion

Git-based module sharing with semantic versioning and GitHub releases is the most accessible approach for most organizations. Automate validation and documentation checks in CI to ensure every released module is tested and documented. As your module library grows, consider moving to a private registry for better discoverability and access control.
