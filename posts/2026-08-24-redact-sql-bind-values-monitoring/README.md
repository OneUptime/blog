# Redact SQL Text and Bind Values Before Shared Monitoring

Author: [nawazdhandala](https://www.github.com/nawazdhandala)

Tags: Database Monitoring, OpenTelemetry, Data Redaction, SQL Security, Privacy

Description: Minimize database-monitoring exposure by capturing normalized query identity, suppressing bind values, and enforcing a tested collector-side deny policy before shared export.

---

Database telemetry frequently crosses a larger trust boundary than the database itself. Raw SQL can contain literals, comments, customer identifiers, access tokens, or business data; bind attributes contain the values that parameterization intentionally kept out of the statement.

The safest control is data minimization at the instrumentation source. A collector-side rule is a second boundary, not permission to capture secrets everywhere upstream.

## Define the allowed data product

For fleet monitoring, the useful minimum is usually:

- database system and logical service identity;
- low-cardinality operation name and query summary;
- a normalized digest or fingerprint generated by a real parser;
- duration, count, error class, row count, and resource metrics;
- tightly controlled database, schema, table, or collection identifiers.

Do not export bind values by default. Do not use raw SQL as a metric label: it exposes data and creates unbounded cardinality.

OpenTelemetry's database span conventions make the distinction explicit. `db.query.summary` should be low-cardinality and contain no dynamic or sensitive data. Non-parameterized `db.query.text` should be collected by default only when all literals have been sanitized. `db.query.parameter.<key>` is a Development, opt-in attribute, and instrumentation should not capture it by default because it can contain PII or secrets.

## Sanitize SQL with a parser, not a regular expression

A compliant sanitizer replaces every literal type-not just quoted strings-with a placeholder such as `?`. That includes numeric, date/time, Boolean, interval, binary, and hexadecimal literals. It also needs to handle dialect-specific quoting, dollar strings, escaped text, comments, nested statements, and malformed input.

For example:

```sql
SELECT *
FROM customer_orders
WHERE email = 'alice@example.com'
  AND created_at >= TIMESTAMP '2026-08-01 00:00:00'
  AND tenant_id = 4815;
```

should become a shape such as:

```sql
SELECT *
FROM customer_orders
WHERE email = ?
  AND created_at >= ?
  AND tenant_id = ?;
```

OpenTelemetry says already parameterized query text should not be sanitized; its placeholders are valuable, while the parameter attributes are the sensitive part. A parser failure must fail closed: omit query text or replace it with a constant, record a redaction-failure counter, and retain only a policy-approved keyed fingerprint for internal correlation if operationally necessary.

Normalization does not guarantee anonymity. Object names and SQL comments may identify tenants or reveal business operations. MySQL digests replace literals but retain identifiers; PostgreSQL normalized statements and other vendor fingerprints have their own version-specific rules. Threat-model each source rather than calling every digest “safe.”

## Enforce a collector deny boundary

If spans can arrive with unsafe attributes, the following OpenTelemetry Collector Contrib transform-processor excerpt can remove them before the shared exporter. The receiver, batch processor, and exporter definitions are omitted:

```yaml
processors:
  transform/redact_database:
    error_mode: propagate
    trace_statements:
      - delete_key(span.attributes, "db.query.text")
      - delete_key(span.attributes, "db.statement")
      - delete_matching_keys(span.attributes, "^db\\.query\\.parameter\\.")

service:
  pipelines:
    traces:
      receivers: [otlp]
      processors: [transform/redact_database, batch]
      exporters: [otlp_grpc/shared_backend]
```

`db.statement` is the legacy attribute that may coexist during semantic-convention migration. The regex removes all keys under the `db.query.parameter.` prefix instead of guessing parameter names.

This policy intentionally drops all query text. A more permissive pipeline may retain `db.query.text` only when the producing instrumentation is explicitly allowlisted and supplies a verified sanitized marker. Do not try to infer safety from the presence of `?`; a raw query can contain both placeholders and literal secrets.

Pin the collector version and test the exact OTTL syntax. Current transform-processor documentation applies its modern configuration style to releases 0.120.0 and later, and invalid OTTL prevents the Collector from loading the configuration. With `error_mode: propagate`, a runtime transformation error is returned up the pipeline and the affected payload is dropped instead of being forwarded; decide and test the desired failure behavior for your availability model.

## Handle metrics, logs, and vendor data too

Redaction is not only a tracing problem:

- remove SQL text and parameters from metric attributes before series creation;
- configure database slow logs and audit logs with their own redaction controls;
- inspect exception messages and stack traces for echoed SQL or values;
- suppress URLs containing credentials and connection strings;
- avoid recording plan XML/JSON or profiler documents in a broadly accessible log sink;
- review exemplars because they can preserve filtered attributes and link metrics to detailed traces.

Hashing a bind value is often unsafe. Emails, IDs, postal codes, and Boolean values can be recovered by dictionary attack, and a stable hash enables cross-service tracking. Prefer deletion or an approved keyed-tokenization service with rotation and access controls.

## Prove the boundary continuously

Build a test corpus containing every supported SQL dialect and secret class: strings, numbers, timestamps, UUIDs, comments, byte literals, arrays, `IN` lists, prepared statements, stored-procedure calls, parse failures, and batches. Send it through the exact production pipeline and assert that the egress payload contains only approved fields.

Add canary secrets that must never appear downstream, redaction-failure metrics, sampled schema inspection, and configuration-change review. Test all egress paths, including retries, dead-letter queues, debug exporters, collector logs, and support bundles.

Finally, apply ordinary security controls: least-privilege database monitoring users, TLS, endpoint authentication, tenant isolation, short retention, audited access, and deletion procedures. Redaction reduces exposure; it does not make telemetry public data.

## Official Documentation

- [OpenTelemetry database client span semantic conventions](https://opentelemetry.io/docs/specs/semconv/db/database-spans/)
- [OpenTelemetry database semantic-convention migration](https://opentelemetry.io/docs/specs/semconv/non-normative/db-migration/)
- [OpenTelemetry Collector Contrib transform processor](https://github.com/open-telemetry/opentelemetry-collector-contrib/blob/main/processor/transformprocessor/README.md)
- [OpenTelemetry OTTL `delete_matching_keys`](https://github.com/open-telemetry/opentelemetry-collector-contrib/blob/main/pkg/ottl/ottlfuncs/README.md#delete_matching_keys)
- [OpenTelemetry security guidance](https://opentelemetry.io/docs/security/)

## Conclusion

Prefer a parser-generated summary or digest, disable bind capture, and omit query text whenever its sanitation cannot be proved. Enforce the same deny policy again at the last collector before shared export, test it with hostile and malformed SQL, and monitor failures so sensitive database data never depends on a best-effort regular expression.
